#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""README — /dental 랜딩 수리 · 금액 밴드 확정 기록."""
import sys, io, os

P = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'README.md')
s = io.open(P, encoding='utf-8').read()

# 기존 TODO 경고 섹션을 해결 기록으로 교체
OLD_START = '## ⚠️ TODO — 금액을 채워야 한다'
OLD_END = '\n---\n\n## 홈 섹션 순서 (v2 최종)'
i = s.find(OLD_START)
j = s.find(OLD_END)
if i < 0 or j < 0 or j < i:
    sys.exit('FAIL: TODO 섹션 경계를 찾지 못함')

NEW = '''## 금액 밴드 — 채웠습니다 (지어내지 않았습니다)

FAQ 3번의 `TODO` 5칸을 채웠습니다. **출처는 이 패키지 안에 이미 있었습니다.**
`/dental` 랜딩(`pages/치과_홈페이지_제작.html`)이 패키지 표로 정상가를 공개하고 있습니다.

| 패키지 | 정상가 | 기간 | 페이지 | 무상수정 |
|---|---|---|---|---|
| STANDARD · 단일 페이지 | **150만원** | 2주 | 1 | 2회 |
| DELUXE · 진료과목 분리 | **200만원** | 3주 | 7 | 3회 |
| PREMIUM · 콘텐츠 구조까지 | **320만원** | 4주 | 15 | 5회 |

유지비도 같이 적었습니다 — 씨클로에 내는 월 이용료 0원, 도메인·서버 실비 연 20~40만원
(병원 명의 직접 결제), 오픈 후 3개월 무상 점검. 전부 `/dental` 에 있던 문장입니다.

**정상가를 적은 이유.** "예산 밴드 공개로 가자" 고 정하셨기 때문입니다.
런칭 특별가(선착순 10팀, 53~55% 할인)는 지우지 않고 밴드 아래 한 줄로 안내하며
`/dental` 로 넘깁니다. 신뢰의 근거를 마감 압박에 두지 않으면서, 진행 중인 할인은 살립니다.

기존 초안의 세 번째 구간(`다지점·다국어`)은 실제로 판매 중인 상품이 아니라
PREMIUM 으로 교체했습니다.

> 남은 `TODO` 는 About `/COMPANY` 의 **대표 · 사업자등록번호 · 소재지 3칸**뿐입니다.

'''

s = s[:i] + NEW + s[j+1:]

s = s.rstrip('\n') + '''

---

## `/dental` 랜딩 수리 (76.7KB · 가장 큰 페이지)

홈 `#work` 의 Featured 카드가 가리키는 페이지인데, 실측해 보니 **상태가 가장 나빴습니다.**

### 모바일에서 페이지가 무너져 있었습니다

| 뷰포트 | 가로 오버플로 | 뷰포트보다 넓은 노드 |
|---|---|---|
| 390px | **470px** | **276개** |
| 768px | 92px | 23개 |
| 1440px | 0 | 0 |

`.page{width:860px}` 고정폭에 미디어쿼리가 카톡 버튼용 2개뿐이었습니다.
휴대폰에서 페이지 전체가 옆으로 밀리고 글이 잘립니다. 제목은 390px 화면에 88px 로 들어갑니다.

**조치** — `width:min(860px,100%)` · `--P` 를 `clamp(20px,5.4vw,64px)` 로 ·
타이포 사다리 5단(88/66/48/32/19px)을 전부 `clamp()` 로 · 섹션 패딩과 브랜드 마크도 같이.

### 그리고 성능 부채

| 항목 | 전 | 후 |
|---|---|---|
| 가로 오버플로 (390) | 470px | **0** |
| 최대 제목 크기 (390) | 88px | **38px** |
| `will-change` 노드 | **33** | **0** |
| 무한 애니메이션 | 3 (bob · pulse · spin) | **0** |
| Pretendard 로드 | **2벌** (헤드 + 이 페이지가 jsdelivr 정적 전체) | 1벌 |
| 카카오 플로팅 버튼 | **2개** (전역 + 페이지 자체) | 1개 |

- `will-change:transform,opacity` 가 `.scr.snap > *` 에 걸려 상시 합성 레이어 33개를
  만들고 있었습니다. 리빌은 한 번 끝나면 다시 안 일어나므로 미리 잡아둘 이유가 없습니다.
- `pulse` 는 `box-shadow` 애니메이션이라 합성이 아니라 **매 프레임 페인트**를 유발하고,
  무한 반복이라 페이지를 열어 두는 내내 돕니다. 정적 링으로 바꿨습니다.
- Pretendard 정적 전체 CSS 를 jsdelivr 에서 한 번 더 받고 있었습니다. 헤드 스니펫의
  dynamic-subset 과 완전 중복이라 지웠습니다.

### 작동한 적 없던 코드 두 개

```css
#ciclo-dental html{scroll-snap-type:y proximity}   /* html 은 div 의 자손이 될 수 없다 */
```
스크롤 스냅은 **한 번도 적용된 적이 없습니다**(실측 `scrollSnapType:none`).
되살리는 대신 걷어냈습니다 — 20개 장면짜리 긴 랜딩에서 스냅은 모바일 스크롤을 뺏습니다.
`.scr.snap` 의 `min-height` 는 남겨 장면감을 유지했고, `100svh` 를 같이 넣었습니다.

하단에 스코프 없는 `.kko-fab` `<style>` 이 한 벌 더 있었습니다(다른 페이지로 샐 수 있음). 제거.

### 연락처가 사이트 나머지와 달랐습니다

```
/dental 푸터:  kajam0623@gmail.com · 010-8817-2001
나머지 전부:   kajam0623@naver.com · 010-8017-2001
```
전화번호는 한 자리(80→88) 차이라 오타로 보입니다. 헤더·사이드바·푸터·Contact 4곳을
정본으로 보고 맞췄고, 링크(`mailto:`/`tel:`)도 걸었습니다. **번호가 반대라면 알려주세요.**

### 상단 브랜드 줄이 사이트 네비에 덮여 있었습니다

단독 페이지로 만들어진 흔적입니다. 자기 브랜드 줄(`.top .id`)이 고정 네비 알약과
같은 자리에 있어 로고 글자가 가렸습니다. 브랜드 줄을 감추고 h1 이 네비 아래에서
시작하도록 상단 여백을 줬습니다.

### 리빌은 그대로 뒀습니다

메인 사이트의 리빌은 22개 중 5개가 영구히 안 보이는 버그가 있어 삭제했지만,
이 페이지의 리빌 126개는 **1440/390 양쪽에서 전부 정상 발화**하는 것을 확인했습니다
(끝까지 스크롤 후 잔여 0개). 장면 단위 캠페인 랜딩의 의도된 연출이라 남겼습니다.
'''

io.open(P, 'w', encoding='utf-8').write(s + '\n')
print('ok  README.md')

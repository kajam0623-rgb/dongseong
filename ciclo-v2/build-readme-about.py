#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""README — About · Contact 전환 기록 추가."""
import io, os

P = os.path.join(os.path.dirname(os.path.abspath(__file__)), 'README.md')
s = io.open(P, encoding='utf-8').read().rstrip('\n')

s += """

---

## About · Contact (완료) — 마지막 두 페이지

### About

| 문제 | 조치 |
|---|---|
| `.sec-h2` 인라인 `font-size` 3곳 (64px·52px) | `.sec-h2.xl` 클래스 신설 + 시스템 40px 복귀 |
| 본문 문단 인라인 스타일 1곳 | `.sec-lead` 로 편입 → **전 페이지 인라인 font-size 0건** |
| h2 `일하는 기준` (명사구) | `일하는 기준을 셋으로 좁혔고, 나머지는 다 여기서 나옵니다` |
| 분량 2.2KB · 자랑만 있음 | 두 섹션 신설 (아래) |

**신설 `/WHAT WE DON'T DO` — 하지 않는 일 4가지.**
238LAB About 의 핵심은 자랑이 아니라 거절 기준입니다. 고를 때 필요한 정보이기도 합니다.

1. 검색 순위를 보장하지 않습니다
2. 환자 후기와 전후 사진을 쓰지 않습니다 — 의료법. 효과 좋은 소재인 걸 알지만 제안 자체를 하지 않는다고 씀
3. 진단 없이 새로 만들자고 하지 않습니다 — 한 항목만 고쳐 32→46점 된 실측을 근거로
4. 설명할 수 없는 작업은 청구하지 않습니다 — 이관 가능하게 만든다까지 명시

**신설 `/RECORD` — 공개해 둔 것 4가지 + `/COMPANY`.**
채점 기준 50개 · 실태조사 8곳 · 맡은 사이트 실측값 · 못 하는 말.
회사 정보 표를 붙이고 캡션에 "사업자 정보를 적어두지 않은 에이전시는 거르셔도 됩니다" 를 넣었습니다.

> ⚠️ `/COMPANY` 의 **대표 · 사업자등록번호 · 소재지 3칸이 `TODO`** 입니다.
> 알 수 없는 값이라 비웠습니다. FAQ 금액 밴드와 같은 빨간 배지입니다.

### Contact

| 문제 | 조치 |
|---|---|
| 프로세스 제목이 클래스 없는 `<h2>` (타이포 시스템 밖) | `.sec-h2` 편입 + 문장형 (`.contact-proc h2` 22px 는 카드 제목이라 유지) |
| "무료 진단" 이라고만 하고 내용이 없음 | `/FREE CHECK` 섹션 신설 — 홈에서 공개한 쉰 개 기준을 그대로 쓴다고 연결 |
| 무엇을 써 보내야 할지 모름 | `/BEFORE YOU WRITE` 신설 — 4가지, "없어도 괜찮습니다" 를 리드에 명시 |
| 히어로 리드가 "무엇이든 편하게" | "주소 하나만 주셔도 저희가 먼저 채점해서 답장드립니다" — 첫 행동을 하나로 |

### 검증 (About · Contact × 1440 / 390)

| 항목 | 결과 |
|---|---|
| h2 | About 64px(사명)/40px · Contact 22px(카드)/40px |
| 인라인 font-size | **0건** |
| 제목 단어 붙음 | 0 |
| 가로 오버플로 | 0 |
| CLS | 0 |
| JS 에러 | 0 |
"""

io.open(P, 'w', encoding='utf-8').write(s + '\n')
print('ok  README.md')

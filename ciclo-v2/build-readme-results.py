#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""README 갱신 — 실적 밴드(#results) · 제목 <br> 공백 수정 기록."""
import sys, io, os

HERE = os.path.dirname(os.path.abspath(__file__))
P = os.path.join(HERE, 'README.md')

s = io.open(P, encoding='utf-8').read()

OLD_ORDER = """| 5 | 사례 `#work` | **진단 카드 6장** + 리드·링크·출처 |
| 6 | 프로세스 `#process` | h2 한글화 + 리드 |
| 7 | **Why now** `#why` | 신설 |
| 8 | 소개 `#about` | h2 한글화 + 1단 전환 |
| 9 | FAQ `#faq` | **5 → 7문항**, 답변 밀도 상향 |
| 10 | 푸터 | h2 한글화 |"""

NEW_ORDER = """| 5 | 사례 `#work` | **진단 카드 6장** + 리드·링크·출처 |
| 6 | **실적 밴드** `#results` | 신설 (클라이언트 실측) |
| 7 | 프로세스 `#process` | h2 한글화 + 리드 |
| 8 | **Why now** `#why` | 신설 |
| 9 | 소개 `#about` | h2 한글화 + 1단 전환 |
| 10 | FAQ `#faq` | **5 → 7문항**, 답변 밀도 상향 |
| 11 | 푸터 | h2 한글화 |"""

RESULTS_DOC = """

---

## 실적 밴드 `#results` (신설)

지금까지 사이트가 하던 말은 **"남의 사이트를 채점했다"** 까지였습니다.
보내주신 캡처 5장으로 **"맡은 뒤 숫자가 어디로 갔다"** 를 처음 붙였습니다.
플랜 A-3 의 "실적이 없다면 가상 사례를 만들지 말 것" 제약이 풀린 자리입니다.

### 넣은 값

| 구분 | 대상 | 값 |
|---|---|---|
| 네이버 서치어드바이저 (최근 90일) | 치과 ① 수도권·종합진료 | 클릭 **740** · 노출 6.4만 · CTR 1.2% · 직전 대비 +12,300% |
| | 치과 ② 인천·사랑니 중심 | 클릭 **110** · 노출 1.9만 · CTR 0.6% · +1,733% |
| | 치과 ③ 수도권·개원 초기 | 클릭 **74** · 노출 1.4만 · CTR 0.5% · +7,300% |
| PageSpeed Insights | 치과 ④ 모바일 | **100/100/100/100** · FCP 1.0s · LCP 1.7s · TBT 0ms · CLS 0.003 · SI 1.8s |
| | 치과 ⑤ 데스크톱 | **100/100/100/100** · FCP 0.4s · LCP 0.6s · TBT 10ms · CLS 0.006 · SI 0.6s |

배치: 홈 `#work` 바로 뒤(진단 → 결과 → 방법 순서), Work 페이지는 **맨 앞**.
Work 의 기존 실태조사 블록은 라벨이 겹치지 않게 `/RESULT` → **`/SURVEY`** 로 바꿨습니다.

### 캡처 이미지를 붙이지 않고 HTML/CSS 로 재현한 이유

1. **모자이크 요청을 확실히 지킵니다.** 병원명·도메인·주소가 화면에 남을 여지가 없습니다.
2. **전송량이 0 입니다.** 캡처 5장을 붙이면 LCP 후보가 5개 늘고 PSI 100점 목표와 정면으로 부딪힙니다.
3. 확대해도 깨지지 않고, 스크린리더가 읽고, 숫자를 고칠 때 이미지를 다시 만들 필요가 없습니다.

원본 캡처를 그대로 노출하고 싶으시면 파일을 저장소에 넣어 주세요.
지금 대화에 올려주신 5장은 **제 화면에서는 보이지만 디스크에는 없어서** 가공할 수 없었습니다.

### 238LAB 문법 적용 (숫자보다 이게 핵심입니다)

증가율 +12,300% 를 크게 쓰고 끝내면 그건 광고 문법입니다. 같은 블록 안에 이렇게 적었습니다:

> 세 곳 모두 직전 90일 클릭이 한 자리였습니다. 740클릭은 6클릭에서 온 값이고,
> 74클릭은 1클릭에서 온 값입니다. 배수만 보시면 실제 크기를 잘못 읽게 됩니다.

- 카드 안에 **`직전 90일 클릭` 행**을 따로 뒀습니다. 배수의 분모를 숨기지 않습니다.
- 캡션에 **"클릭률 1.2%는 업계 평균을 넘는 값이 아닙니다"** 를 넣었습니다.
- PSI 100점 옆에 **"빠르다는 뜻이지 잘 팔린다는 뜻이 아닙니다"** 를 붙였습니다.
- Work `/LIMITS` 에 "같은 결과를 약속드리지 않습니다" 한 줄을 추가했습니다.

### ⚠️ 배포 전 확인이 필요한 것 — 귀속 표현

`/RESULT · **저희가 맡은** 사이트`, `페이지 속도 — **저희가 만든** 사이트 실측` 으로 썼습니다.
캡처의 성격(서치어드바이저 계정 화면 + 자사 실적 맥락)으로 판단한 것이라,
**다섯 곳 전부가 씨클로 작업분이 맞는지 확인해 주세요.**

- 일부만 씨클로 작업이면 → 해당 카드만 남기고 나머지는 빼면 됩니다.
- 유지보수만 맡은 곳이 섞여 있으면 → "맡은" 을 "운영 중인" 으로 바꾸는 편이 정확합니다.
- 치과 ② 의 `인천 · 사랑니 중심`, ③ 의 `개원 초기` 같은 태그도 사실과 다르면 알려주세요.

숫자 자체는 캡처에 찍힌 값 그대로이고, 제가 보정하거나 반올림한 것은
노출(6.4만/1.9만/1.4만)과 직전 90일 클릭(증가율에서 역산한 `약 6`·`약 6`·`약 1`) 두 가지뿐입니다.

---

## 제목 `<br>` 앞 공백 — 420px 이하 단어 붙음 버그 (수정)

`@media(max-width:420px){.sec-h2 br{display:none}}` 로 좁은 화면에서 강제 개행을 푸는데,
마크업이 `않았습니다,<br>고친` 이라 br 이 사라지면 **`않았습니다,고친`** 으로 붙었습니다.
홈·서브 전 페이지 제목 **48곳**에서 나던 증상입니다.

CSS 로 해결되지 않는다는 것을 먼저 확인했습니다:

| 시도 | 결과 |
|---|---|
| `br::after{content:" "}` | 렌더되지 않음 |
| `br{display:inline;content:" "}` | br 이 그대로 개행 |
| 마크업 `, <br>` | ✅ 데스크톱 줄 폭 501/128px 로 **변화 없음**, 모바일 정상 |

`build-br-space.py` 로 h1·h2 안의 `<br>` 에만 공백을 넣었습니다.
카드 안 `<br>`(`.psi-s small` 등)은 숨기는 규칙이 없어 건드리지 않았습니다.

### 실적 밴드 검증 (Home / Work × 1440 / 768 / 390)

| 항목 | 결과 |
|---|---|
| 유입 카드 | 3 / 2 / 1 열 |
| PSI 링 | 8개 · 정원(가로=세로) · 잘림 0 |
| 점수 라벨 밑선 | 4개 정렬 (2줄 최소 높이) |
| 코어 지표 | 5 / 5 / 3 열 · 잘림 0 |
| h2 | 40 / 28 / 26px · 2줄 |
| Work 표 | 1440 넘침 0 · 390 가로 스크롤 310px |
| 가로 오버플로 | **0** |
| CLS | **0** |
| 실행 중 애니메이션 | 1 (marquee) |
| `will-change` | 0 |
| JS 에러 | **0** |
"""

if s.count(OLD_ORDER) != 1:
    sys.exit('FAIL: 섹션 순서 표 앵커를 찾지 못함')
s = s.replace(OLD_ORDER, NEW_ORDER)
s = s.rstrip('\n') + '\n' + RESULTS_DOC

io.open(P, 'w', encoding='utf-8').write(s)
print('ok  README.md')
